[build-system]
requires = ["setuptools>=81.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "LWGNet"
version = "1.0.0"
dependencies = [
  "matplotlib>=3.10.9",
  "scipy>=1.17.1",
  "torch>=2.6.0",
]
